@model IEnumerable<BentoLab.Models.Torta>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Naziv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KolicinaNaStanju)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dostupna)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naziv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KolicinaNaStanju)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dostupna)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TortaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TortaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TortaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
